{
  "name": "hey-pacer",
  "version": "1.0.0",
  "description": "Pacer Search static site",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
